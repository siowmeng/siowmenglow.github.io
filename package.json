{
  "name": "astro",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "preview": "astro preview",
    "astro": "astro",
    "cv:build": "tectonic -o public cv/main.tex --keep-logs && mv public/main.pdf public/cv.pdf",
    "build": "npm run cv:build && astro build"
  },
  "dependencies": {
    "astro": "^5.17.1"
  }
}
